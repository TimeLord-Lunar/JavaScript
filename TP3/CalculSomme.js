// // // VARIABLES INITIALIZATION // // //

// Numeric Variables
let nombre = 10;
let somme = 0;

// Loop to calculate the sum of all numbers between 1 and 10 included
for (let i = 1; i <= nombre; i++) 
{
    somme += i;
}

// Display Result
console.log("La somme de tous les nombres de 1 à", nombre, "est :", somme);