// // // VARIABLES INITIALIZATION // // //

// Array Variables
let array = [1, 3, 5, 1];

// Boolean Value Calculation
let result = array.length >= 1 && array[0] === array[array.length - 1];

// Display result
console.log(result);
